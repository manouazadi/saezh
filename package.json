{
  "name": "vanilla-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun scripts/generate-photos.js && tsc && vite build",
    "preview": "vite preview",
    "photos": "bun scripts/generate-photos.js"
  },
  "devDependencies": {
    "reset-css": "^5.0.2",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vlitejs": "^8.0.0"
  },
  "dependencies": {
    "exif-js": "^2.3.0",
    "gsap": "^3.13.0"
  }
}
